import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gsl.app',
  appName: 'FacturacionGSL',
  webDir: 'dist/fact-electr-cnr'
};

export default config;